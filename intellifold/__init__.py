"""IntelliFold: a controllable foundation model for biomolecular structure prediction.

Inference runs on the AlphaFold 3 JAX engine (see `intellifold.cli` /
`intellifold.run_jax_inference`); the native PyTorch stack lives under
`intellifold.openfold` / `intellifold.data`.
"""

__version__ = '2.0.2'
